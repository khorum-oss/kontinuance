// Standalone target application for the Kontinuance demo — NOT part of the Kontinuance multi-module build
// (it is intentionally absent from the root settings.gradle.kts). Kontinuance treats it exactly like any
// external application repo: it checks it out and runs its Gradle tasks.
rootProject.name = "kontinuance-sandbox"
