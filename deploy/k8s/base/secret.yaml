apiVersion: v1
kind: Secret
metadata:
  name: secrets
type: Opaque
stringData:
  # Placeholder pipeline secret(s) referenced by the descriptor's steps. Set real values via your secret
  # manager before production — a secret-operator integration (external-secrets / 1Password) is a later
  # slice. Never commit real values here.
  DEPLOY_TOKEN: "changeme"
