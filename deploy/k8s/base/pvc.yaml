apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: runs
spec:
  # Single-writer file-backed run store → ReadWriteOnce and a single server replica.
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
