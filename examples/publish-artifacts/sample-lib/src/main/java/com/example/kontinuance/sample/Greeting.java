package com.example.kontinuance.sample;

/** Trivial class so the sample library produces a real, publishable jar. */
public final class Greeting {

    private Greeting() {
    }

    public static String greet(String name) {
        return "Hello, " + name + "!";
    }
}
