// Standalone build (its own settings) — intentionally NOT part of the kontinuance multi-module build.
// It exists only so the publish-artifacts example can be run and verified end-to-end out of the box.
rootProject.name = "sample-lib"
